upstream task_manager {
    server task_manager:8000;
}

server {

    listen 80;

    location / {
        include proxy_params;
        proxy_pass http://task_manager;
    }

    location /static/ {
        alias /user_tm/static/;
    }

    location /media/ {
        alias /user_tm/media/;
    }
}
